'use strict';

const TeleBot = require('../');

const bot = new TeleBot({
  token: '73107572:AAEEXlxpasqtvIs1pFUul_qVBByt108H8mQ',
  webhook: {
    // Self-signed certificate:
    key: './key.pem',
    cert: './cert.pem',
    url: 'https://83.99.220.240',
    host: '192.168.1.105',
    port: 443
  }
});

bot.on('text', msg => bot.sendMessage(msg.from.id, msg.text));

bot.connect();

